Put your app logo here as `icon.png` (min 1024x1024, square, PNG).

Then run:
  flutter pub get
  flutter pub run flutter_launcher_icons

This generates the launcher icon for Android/Windows/Linux from lib/pubspec.yaml config.
